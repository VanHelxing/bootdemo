<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="van.helsing.bootdemo.product.mapper.ProductMapper">


    <sql id="product_fields">
        ${alias}.id,
        ${alias}.pro_code,
        ${alias}.pro_name,
        ${alias}.pro_type,
        ${alias}.create_by,
        ${alias}.create_time,
        ${alias}.update_by,
        ${alias}.update_time,
        ${alias}.remark
    </sql>
</mapper>